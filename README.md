# Analyze-PE — Анализатор PE-файлов (EXE/DLL)

Быстрый инструмент для исследования PE-файлов на Go. Идеален для анализа вредоноса, проверки сигнатур и обратного проектирования.

**Основные возможности:**
- 📊 Базовая информация: хэши (SHA256, SSDEEP), размер, архитектура
- 📦 Секции: имена, размеры, энтропия, флаги
- 📚 Импорты: DLL и функции
- 🔍 Отладочная информация: тип и версия отладчика
- 📄 Извлечение строк: ASCII и UTF-16LE

---

## 🚀 Быстрый старт

### Установка

```bash
git clone https://github.com/Sccrap/analyze-pe.git
cd analyze-pe
go build -o analyzer
```

### Использование

```bash
# Веб интерфейс (рекомендуется)
./analyzer --web              # Откроется http://localhost:8080
./analyzer -w                 # Короткий флаг

# Базовая информация (CLI)
./analyzer file.exe

# С опциями (CLI)
./analyzer -b file.exe        # SHA256, размер, архитектура
./analyzer -s file.exe        # Секции и энтропия
./analyzer -i file.exe        # Импорты
./analyzer -d file.exe        # Отладочная информация
./analyzer -x file.exe        # Извлечь строки в stdout
./analyzer -x file.exe out.txt # Сохранить строки в файл
```

---

## 🌐 Веб интерфейс

Запустите веб-интерфейс для интуитивного анализа PE-файлов:

```bash
./analyzer --web
```

Автоматически откроется: **http://localhost:8080**

**Возможности веб-интерфейса:**
- 📁 Загрузка файла через форму
- 🎯 Кнопки для быстрого анализа
- 📊 Красивый вывод результатов
- 🎨 Тёмная тема с подсветкой
- ✓ Полная функциональность на любой ОС

**Преимущества:**
- ✅ Работает везде (Windows, macOS, Linux)
- ✅ Не требует зависимостей (только Go)
- ✅ Кросс-платформенная сборка
- ✅ Легко развертывается

---

## 📖 Команды

| Флаг | Описание |
|------|---------|
| `-h, --help` | Справка |
| `-w, --web` | Веб интерфейс (http://localhost:8080) |
| `-b, --basic` | Хэши, размер, архитектура, кол-во секций |
| `-s, --sections` | Секции, виртуальные размеры, энтропия |
| `-i, --imports` | Таблица импортов (DLL → функции) |
| `-d, --debug` | Отладочная информация (CodeView, PDB, PGO) |
| `-x, --strings` | Извлечь ASCII/UTF-16LE строки |

**Примеры вывода:**

```bash
$ ./analyzer -b malware.exe
Basic info for malware.exe:
--------------------------------------
File size:   512000 bytes
Machine:     x86 (Intel 80386)
SHA256:      a1b2c3d4...
SSDEEP:      12288:ABC...
Sections:    3
--------------------------------------
```

```bash
$ ./analyzer -s malware.exe
Successfully parsed malware.exe
Number of sections: 3
Section Name: .text
  VirtualSize: 0x4000
  Characteristics: 0x60000020
  Entropy: 7.234

Section Name: .data
  VirtualSize: 0x1000
  Characteristics: 0xc0000040
  Entropy: 2.145
```

```bash
$ ./analyzer -d malware.exe
Debug info for malware.exe:
--------------------------------------
Number of debug entries: 1

Debug Entry 1:
  Type:              IMAGE_DEBUG_TYPE_CODEVIEW
  Characteristics:   0x0
  TimeDateStamp:     1234567890 (0x499602d2)
  MajorVersion:      20
  MinorVersion:      0
  PointerToRawData:  0x1000
  SizeOfData:        200 bytes
  AddressOfRawData:  0x5000
  -> CodeView (PDB) format detected
--------------------------------------
```

---

## 🔧 Технические детали

**Поддерживаемые форматы:** PE32 (x86), PE32+ (x64)

**Основана на:** [`github.com/saferwall/pe`](https://github.com/saferwall/pe)

**Как работает:**
1. Загружает PE файл (`pe.New()`)
2. Парсит все структуры: DOS Header → NT Header → секции → импорты/отладка
3. Извлекает информацию: вычисляет энтропию, хэши, строки
4. Выводит результаты в удобном формате

---

## ⚙️ Требования

- Go 1.18+
- macOS / Linux / Windows

Установка зависимостей:
```bash
go get github.com/glaslos/ssdeep
go get github.com/saferwall/pe
```

## 🛠️ Сборка для разных ОС

Приложение кроссплатформенно и собирается для всех основных ОС с помощью стандартных переменных окружения Go:

```bash
# macOS / Linux (native)
go build -o analyzer

# Windows (64-bit)
GOOS=windows GOARCH=amd64 go build -o analyzer.exe

# Linux (64-bit explicit)
GOOS=linux GOARCH=amd64 go build -o analyzer-linux

# ARM64 (например для Raspberry Pi или Apple Silicon)
GOOS=linux GOARCH=arm64 go build -o analyzer-arm64
```

### Сборка релиза для Windows на macOS

**Вариант 1: Используя fyne-cross (с GUI)**

```bash
# Установите fyne-cross
go install github.com/fyne-io/fyne/cmd/fyne@latest

# Соберите для Windows (требует Docker)
fyne-cross windows -output analyzer.exe

# Бинарник будет в ./fyne-cross/dist/windows/analyzer.exe
```

**Вариант 2: Прямая кросс-компиляция (только CLI, без GUI)**

```bash
# Это работает везде, но без графического интерфейса
GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -o analyzer.exe

# Проверить результат
file analyzer.exe
ls -lh analyzer.exe
```

**Вариант 3: Скрипт для сборки всех релизов**

Создайте файл `build-release.sh`:

```bash
#!/bin/bash

mkdir -p dist

# Windows
GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -o dist/analyzer-windows-amd64.exe

# Linux
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o dist/analyzer-linux-amd64
GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -o dist/analyzer-linux-arm64

# macOS
GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 go build -o dist/analyzer-darwin-amd64
GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -o dist/analyzer-darwin-arm64

# Apple Silicon (M1/M2/M3)
go build -o dist/analyzer-macos-arm64

chmod +x dist/*
ls -lh dist/
```

Запуск:
```bash
chmod +x build-release.sh
./build-release.sh
```

**Примечание о GUI:**
- Если используете вариант 2 или 3 (без CGO), GUI (`--gui`) работать не будет
- Все остальные функции (CLI анализ) работают полностью
- Для Windows с полной функциональностью используйте **Вариант 1** (fyne-cross с Docker)

Примечания:
- GUI (Fyne) требует наличия графической среды при запуске; для кросс-компиляции GUI на Windows/macOS может потребоваться набор инструментов или использование `fyne-cross` для сборки нативных приложений.
- Тесты запускаются так: `go test ./...` и не зависят от GUI.


### Решение проблемы с GOPROXY на Windows

Если при `go mod tidy` или `go build` возникает ошибка вроде **"GOPROXY list is not the empty string, but contains no entries"**, нужно сбросить переменную окружения:

**PowerShell (администратор):**
```powershell
[Environment]::SetEnvironmentVariable("GOPROXY", "https://proxy.golang.org,direct", "User")
```

**Command Prompt (администратор):**
```cmd
setx GOPROXY https://proxy.golang.org,direct
```

**Временное решение (текущая сессия):**
```cmd
set GOPROXY=https://proxy.golang.org,direct
go mod tidy
```

Или просто удалить переменную если она пуста.

### Ошибка "malformed import path "/" и пакеты не найдены на Windows

Если видите ошибку типа **"malformed import path "/" : trailing slash"** и Go не находит стандартные пакеты, проблема в поломанной установке Go (часто после обновлений):

**Решение (PowerShell как администратор):**
```powershell
# 1. Очистить весь кэш модулей
go clean -modcache

# 2. Удалить go.mod и go.sum
Remove-Item go.mod, go.sum -Force -ErrorAction SilentlyContinue

# 3. Удалить всю директорию pkg (если нужно)
Remove-Item $env:USERPROFILE\go\pkg -Recurse -Force -ErrorAction SilentlyContinue

# 4. Переинициализировать модули
go mod init github.com/Sccrap/analyze-pe
go mod tidy

# 5. Проверить работу
go build -o analyzer.exe
```

**Если это не помогает:**
1. Скачайте последнюю стабильную версию Go с [golang.org](https://golang.org/dl/)
2. Удалите текущую версию Go (Программы и компоненты → удалить)
3. Установите заново (в простой путь без специальных символов)
4. Закройте и откройте PowerShell/CMD заново
5. Повторите шаги выше

### Ошибка "build constraints exclude all Go files" (Fyne на Windows)

Если при сборке видите:
```
imports github.com/go-gl/gl/v2.1/gl: build constraints exclude all Go files
```

Это проблема с Fyne GUI на Windows — не хватает графических библиотек (OpenGL/GCC). 

**Решение 1: Установить MinGW/GCC (рекомендуется)**

Скачайте и установите [MSYS2](https://www.msys2.org/):
```powershell
# В PowerShell как администратор
choco install msys2  # или установите вручную с сайта

# В MSYS2 терминале:
pacman -S mingw-w64-x86_64-gcc mingw-w64-x86_64-pkg-config

# Добавьте в PATH:
$env:PATH += ";C:\msys64\mingw64\bin"

# Теперь собирайте:
go build -o analyzer.exe
```

**Решение 2: Использовать fyne-cross (кросс-компиляция)**

```powershell
go install github.com/fyne-io/fyne/cmd/fyne@latest

# Собрать Windows версию с Linux/macOS
fyne-cross windows -output analyzer
```

**Решение 3: Собрать без GUI (только CLI)**

Если GUI не критичен, используйте только CLI версию:
```powershell
# Это работает везде, без GUI зависимостей
go build -o analyzer.exe -tags=nocgo
```

**Решение 4: Использовать готовый бинарь**

Загрузите скомпилированный `analyzer.exe` из [Releases](https://github.com/Sccrap/analyze-pe/releases) (когда будут доступны).



---

## � GitHub Actions: Автоматическая сборка релизов

Проект использует GitHub Actions для автоматической кросс-платформенной сборки.

### Как создавать релизы

**Способ 1: Через Git теги (рекомендуется)**

```bash
# В локальном репозитории
git tag v1.0.0
git push origin v1.0.0
```

GitHub Actions автоматически:
1. Соберёт проект для Windows (GUI), Linux (x64, ARM64), macOS (Intel, ARM64)
2. Создаст Release страницу
3. Загрузит все бинарники в Release

**Способ 2: Ручной запуск**

1. Откройте репозиторий на GitHub
2. Перейдите в **Actions** вкладку
3. Выберите **"Build & Release"** workflow
4. Нажмите **"Run workflow"** → **Run workflow**

### Что собирается в релизе

| Название | ОС | Архитектура | GUI | Размер |
|----------|----|----|-----|--------|
| `Windows-x64-GUI` | Windows | x86_64 | ✅ Да (Fyne) | ~50-80MB |
| `Linux-x64` | Linux | x86_64 | ❌ CLI | ~15-20MB |
| `Linux-ARM64` | Linux | ARM64 | ❌ CLI | ~15-20MB |
| `macOS-Intel` | macOS | x86_64 | ❌ CLI | ~20-25MB |
| `macOS-ARM64` | macOS | ARM64 | ❌ CLI | ~20-25MB |

**Примечание:** Windows версия с GUI требует использования `fyne-cross` на Linux runner, поэтому на других платформах собирается CLI версия.

### Структура workflow

Файл `.github/workflows/build-release.yml` содержит:

- **`build` job:** Собирает для каждой платформы параллельно
- **`release` job:** Создаёт GitHub Release и загружает артефакты

Каждая сборка:
- Использует Go 1.21+
- Отключает CGO для полной кросс-компиляции
- Создаёт исполняемые файлы
- Загружает артефакты для скачивания

### Как скачать релизы

1. Откройте репозиторий: https://github.com/Sccrap/analyze-pe
2. Перейдите в **Releases**
3. Выберите нужную версию
4. Скачайте бинарник для вашей ОС

---

## 🛡️ Безопасность


⚠️ При анализе вредоноса:
- Используйте изолированную VM или контейнер
- Не распаковывайте подозрительные файлы вне песочницы
- Утилита только **читает** файлы, не выполняет их

---

## 📝 Лицензия

MIT License — см. файл `LICENSE`

---

## 🤝 Вклад

Suggestions & PRs приветствуются! Создавайте issues и pull requests.
