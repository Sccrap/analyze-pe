# Analyze-PE — Инструмент статического анализа PE-файлов на Go

`Analyze-PE` — это лёгкая CLI-утилита для статического анализа PE-файлов (EXE/DLL) на языке Go. Основана на библиотеке [`github.com/saferwall/pe`](https://github.com/saferwall/pe) и предназначена для практического использования при анализе вредоносного ПО, сигнатурном исследовании, извлечении структур PE и базовой атрибуции.

Инструмент показывает:
- информацию о заголовках PE;
- количество секций и параметры каждой секции;
- энтропию секций (полезно для выявления упаковщиков);
- таблицу импортов (имена DLL и функций);
- флаги характеристик секций.

---

## 🔧 Возможности
- Чтение PE32 / PE32+ (x86 / x64)
- Устойчивый парсинг на основе `saferwall/pe`
- Вывод структуры PE и параметров секций
- Расчёт энтропии секций
- Извлечение импортов и ординалов
- Защита от кривых/повреждённых PE в Options

---

## 📥 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/<yourname>/analyze-pe.git
cd analyze-pe
```

### 2. Сборка
```bash
go build -o analyze-pe
```

---

## ▶️ Использование

### Базовый запуск
```bash
./analyze-pe sample.exe
```

### Пример вывода
```
Successfully parsed sample.exe
Number of sections: 5
Section Name: .text
  VirtualSize: 0x4f000
  Characteristics: 0x60000020
  Entropy: 6.321

dll: kernel32.dll
  -> CreateFileA
  -> ReadFile
```

---

## 📂 Структура проекта
```
/
 ├── main.go               # Точка входа CLI
 ├── go.mod
 ├── go.sum
 └── README.md             # Документация
```

---

## 🧠 Как работает
1. Программа принимает путь к EXE/DLL.
2. Использует `pe.New()` или `pe.NewFromBytes()` для загрузки.
3. Выполняет безопасный парсинг (`Parse()`), включая:
   - DOS Header
   - NT Headers
   - FileHeader
   - OptionalHeader
   - Section Headers
   - Imports
4. Извлекает секции, считает энтропию и выводит информацию.
5. Отображает таблицу импортов.

---

## ⚙️ Зависимости
Основная зависимость:
```text
github.com/saferwall/pe
```
Автоматически подтягивается через `go mod tidy`.

---

## 🛡️ Безопасность
При работе с подозрительными бинарниками:
- запускайте утилиту в изолированном контейнере или VM;
- не открывайте PE вне песочницы;
- используйте ограниченные `Options` в парсере при массовом анализе.

---

## 🌐 Планы по развитию
- Добавить расчёт `impHash`
- Возможность экспорта данных в JSON
- Поддержка анализа сертификатов
- Детальный вывод CLR (.NET)
- Web-интерфейс

---

## 📝 Лицензия
Проект распространяется под MIT License. См. LICENSE.

---

## 🤝 Вклад в проект
Pull Request'ы приветствуются! Если хотите предложить функции/исправления — создавайте issue.

---

## 📩 Контакты
Если у вас есть предложения или идеи — пишите в Issues или Pull Requests.
